/** @type {import('next').NextConfig} */
const nextConfig = {
    // Lord. 
    // I have sinned. 
    // To the God please forgive thus I am only a human
    typescript:{
        ignoreBuildErrors:true
    }
};

export default nextConfig;
